{
  "name": "mapty-app",
  "version": "1.0.0",
  "description": "application to track your sport activities",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html --dist-dir ./dist",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "glacien",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.10.0"
  }
}
